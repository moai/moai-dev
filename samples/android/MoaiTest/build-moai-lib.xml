<?xml version="1.0" encoding="UTF-8"?>
<project name="MoaiTest.buildLib" default="build">

    <!-- ================================================================ -->
    <!-- build -->
    <!-- ================================================================ -->	
	<target name="build">
		
		<echo level="info" message="Builing Moai..."/>
		
		<exec executable="bash" dir="../../../android-ndk">
			<arg line="build.sh"/>
		</exec>
	</target>

    <!-- ================================================================ -->
    <!-- clean -->
    <!-- ================================================================ -->	
	<target name="clean">
		
		<echo level="info" message="Cleaning Moai..."/>
		
		<exec executable="bash" dir="../../../android-ndk">
			<arg line="clean.sh"/>
		</exec>
	</target>
	
</project>