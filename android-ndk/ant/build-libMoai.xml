<?xml version="1.0" encoding="UTF-8"?>
<project name="MoaiTest.buildLib" default="build">

    <!-- ================================================================ -->
    <!-- build -->
    <!-- ================================================================ -->	
	<target name="build">
		
		<echo level="info" message="Builing libMoai..."/>
		
		<exec executable="bash" dir="..">
			<arg line="build.sh"/>
		</exec>
	</target>

    <!-- ================================================================ -->
    <!-- clean -->
    <!-- ================================================================ -->	
	<target name="clean">
		
		<echo level="info" message="Cleaning libMoai..."/>
		
		<exec executable="bash" dir="..">
			<arg line="clean.sh"/>
		</exec>
	</target>
	
</project>