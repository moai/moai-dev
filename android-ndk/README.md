This builds a default libmoai.so. If you need a custom moai.so for your project then copy the "jni" folder somewhere and make your changes directly to the makefiles.

To build:

cd moai-dev/android-nkd/jni
ndk-build -j4

